package Week9ArraysInJava.Class9point5DifferentDataTypesofArrays;

public class CombineFirstAndSecondWords {
    public static void main(String[] args) {

    }
}
    //The method combineWords takes a String array (array) and an integer (num) as parameters.
//    public static String combineWords(String[] array, int num) {
//        // Check if the array is empty
//
//        if (array.length == 0) {
//
//            //The first if condition checks if the length of the array is 0, and if so, it returns "Array is empty".
//            return "Array is empty";
//        }
//
//        // Check if n is not positive
//        if (num <= 0) {
//            // if so, it returns "N is not positive".
//            return "N is not positive";
//        }
//
//        // Get the first and second words from the array
//        String firstWord = array[0];
//        String secondWord = array[1];
//
//        // Check the lengths of the first and second words
//        if (firstWord.length() < secondWord.length()) {
//            // Repeat the second word n times and concatenate the first word
//            return secondWord.repeat(num).concat(firstWord);
//        } else if (firstWord.length() > secondWord.length()) {
//            // Repeat the first word n times and concatenate the second word
//            return firstWord.repeat(num).concat(secondWord);
//        } else {
//            return "Both are equal"; // Return this if the lengths are equal
//
//        }
//    }
//}

